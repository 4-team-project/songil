<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.takku.project.mapper.ReviewMapper">

	<!-- 리뷰 등록 -->
	<insert id="insertReview" parameterType="ReviewDTO">
		INSERT INTO review (
		review_id, user_id, product_id, rating, content, created_at
		) VALUES (
		review_seq.NEXTVAL, #{userId}, #{productId}, #{rating}, #{content},
		SYSDATE
		)
	</insert>

	<!-- 리뷰 삭제 -->
	<delete id="deleteReview" parameterType="int">
		DELETE FROM review WHERE
		review_id = #{reviewId}
	</delete>

	<!-- 리뷰 수정 -->
	<update id="updateReview" parameterType="ReviewDTO">
		UPDATE review
		SET rating
		= #{rating},
		content = #{content}
		WHERE review_id = #{reviewId}
	</update>

	<!-- 메뉴id 같은걸로 리뷰보기 -->
	<select id="reviewByProductId" parameterType="int"
		resultType="ReviewDTO">
		SELECT *
		FROM review
		WHERE product_id = #{productId}
		ORDER BY created_at DESC
	</select>
	<!-- 내 리뷰 조회 -->
	<select id="reviewByUserID" parameterType="int"
		resultType="ReviewDTO">
		SELECT *
		FROM review
		WHERE user_id = #{userId}
		ORDER BY created_at DESC
	</select>
</mapper>
	